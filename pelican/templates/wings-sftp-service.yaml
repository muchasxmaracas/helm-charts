apiVersion: v1
kind: Service
metadata:
  name: wings-sftp
  namespace: pelican
spec:
  type: LoadBalancer
  selector:
    app: wings
  ports:
    - name: sftp
      protocol: TCP
      port: 2022
      targetPort: 2022