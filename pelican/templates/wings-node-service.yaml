apiVersion: v1
kind: Service
metadata:
  name: wings-node-1
  namespace: pelican
spec:
  selector:
    app: wings
    statefulset.kubernetes.io/pod-name: wings-0
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080